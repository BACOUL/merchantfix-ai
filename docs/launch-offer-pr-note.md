# Launch offer PR note

The simplified launch offer PR is stacked on top of `sales-readiness-cleanup`.

Merge order:

1. `sales-readiness-cleanup`
2. `simplify-launch-offer`

Do not merge this branch into `main` before the sales readiness cleanup branch unless GitHub shows the final diff as expected.
